var arr = new Array(100, 200, 300, 400, 500, 600);
console.log(" The elements of array are: " + arr);
console.log(" \nThe length of the array is: " + arr.length);

// Find the number of characters in a string

var str = "Welcome to the coruscate.com";
var arr = new Array();
arr = str.split(" ");
console.log(" The given string is: " + str);
console.log("\n Number Of Words: " + arr.length);
console.log("\n Number of characters in the string: " + str.length);
