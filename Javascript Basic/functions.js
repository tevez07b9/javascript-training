// Run these with Nodejs
// Eg: node <filename>

// Functions
function addTwoNumbers(x, y) {
    return x + y;
}
console.log("logging a function output...", addTwoNumbers(1, 3));

// Function Parameters
function square(number) {
    return number * number;
}

console.log("logging a parameterized function output...", square(3));
